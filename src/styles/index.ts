interface IColors {
  primary: string;
  white: string;
  blue: string;
  red: string;
}

const colors: IColors = {
  primary: "#581845",
  white: "#f3f2f2",
  blue: "#3f51b5",
  red: "#ed0c0c",
};

export default colors;
